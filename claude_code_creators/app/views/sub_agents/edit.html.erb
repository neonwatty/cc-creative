<div class="container mx-auto px-4">
  <h1 class="text-3xl font-bold mb-6">Edit Sub-Agent</h1>

  <%= render 'form', sub_agent: @sub_agent %>

  <div class="mt-6 flex gap-2">
    <%= link_to "Show", document_sub_agent_path(@document, @sub_agent), class: "btn" %>
    <%= link_to "Back", document_sub_agents_path(@document), class: "btn" %>
  </div>
</div>